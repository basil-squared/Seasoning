--[[
    Given an assets folder, take all sprite images within
    and auto configure them based on the naming scheme of the file.

    Format should go ~something like this
    [NAME_OF_ATLAS]_PX_PY_[ANIM/NORM]

]]



function SEAS.FUNC.CreateAtlasFromFolder(folder)
end
